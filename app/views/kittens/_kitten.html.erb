<div class="kitten">
  <%= link_to (image_tag kitten.image), kitten_categorization_path(kitten, kitten) %>

  <% kitten.categories.each do |category| %>
    <p><%= category.name %></p>
  <% end %>

</div>
